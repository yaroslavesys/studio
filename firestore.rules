rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // --- Helper Functions ---
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return request.auth.uid == userId;
    }
    
    function getRole(userId) {
      return get(/databases/$(database)/documents/users/$(userId)).data.role;
    }
    
    function getRequestingUserRole() {
      return getRole(request.auth.uid);
    }

    // --- Rules ---

    // USERS
    match /users/{userId} {
      // List: Only Admins can list all users. This relies on the client query being correct.
      // The rule checks if the *requesting user's* document has the Admin role.
      allow list: if isSignedIn() && getRequestingUserRole() == 'Admin';
      
      // Get: Any signed-in user can get any user doc. This is needed to resolve user names from IDs.
      allow get: if isSignedIn();
      
      // Create: Any authenticated user can create THEIR OWN user document.
      // This is crucial for the first sign-in.
      allow create: if isSignedIn() && isOwner(userId);

      // Update: Users can update their own doc. Admins can update any.
      allow update: if isSignedIn() && (isOwner(userId) || getRequestingUserRole() == 'Admin');

      // Delete: Not allowed for now to prevent accidental data loss.
      allow delete: if false;
    }

    // DEPARTMENTS
    // Any signed-in user can read departments. Only Admins can write.
    match /departments/{departmentId} {
      allow read: if isSignedIn();
      allow write: if isSignedIn() && getRequestingUserRole() == 'Admin';
    }

    // ACCESS REQUESTS
    match /accessRequests/{accessRequestId} {
      // Create: Users can create requests for themselves.
      allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;

      // List(Query): 
      // - Admins can list all requests.
      // - TechLeads can query requests for their department.
      // - Users can query their own requests.
      // The client-side code is responsible for building the correct query. These rules enforce it.
      allow list: if isSignedIn() && (
        getRequestingUserRole() == 'Admin' ||
        (getRequestingUserRole() == 'TechLead' && request.query.departmentId == get(/databases/$(database)/documents/users/$(request.auth.uid)).data.departmentId) ||
        (request.query.userId == request.auth.uid)
      );

      // Read (Get):
      // - Users can read their own requests.
      // - TechLeads can read requests for their department.
      // - Admins can read all requests.
      allow get: if isSignedIn() && (
        resource.data.userId == request.auth.uid ||
        (getRequestingUserRole() == 'TechLead' && resource.data.departmentId == get(/databases/$(database)/documents/users/$(request.auth.uid)).data.departmentId) ||
        getRequestingUserRole() == 'Admin'
      );
      
      // Update:
      // - TechLeads can update requests in their department.
      // - Admins can update any request.
      // - Users cannot update their own requests once submitted.
      allow update: if isSignedIn() && (
         getRequestingUserRole() == 'Admin' ||
        (getRequestingUserRole() == 'TechLead' && get(/databases/$(database)/documents/accessRequests/$(accessRequestId)).data.departmentId == get(/databases/$(database)/documents/users/$(request.auth.uid)).data.departmentId)
      );
                      
      // Delete: Only admins can delete.
      allow delete: if isSignedIn() && getRequestingUserRole() == 'Admin';
    }
    
    // Meta collection for seeding info
    match /meta/{docId} {
        allow read, write: if true; // Allow anyone to read/write for seeding purposes.
    }
  }
}
